# Application logs

The backend creates `app.log` automatically when it handles requests.

Logs are also printed in the backend terminal.

Sensitive information such as passwords, API keys, tokens, balances, and request bodies is not written to the application log.
